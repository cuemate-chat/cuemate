{
  "error": {
    "apiKeyRequired": "{provider} API key is required",
    "baseUrlRequired": "{provider} requires base_url in credentials",
    "credentialsRequired": "{provider} requires {fields} in credentials",
    "apiError": "{provider} API error: {status} {message}",
    "healthCheckFailed": "{provider} health check failed: {status} {message}",
    "noResponseBody": "No response body"
  }
}
